#include <iostream>
#include <bitset>
#include <string>
using namespace std;

int main(int argc, char const *argv[])
{
    int n;
    std::cin >> n;
    int input;
    for (int i = 0; i < n-1; ++i)
    {
        std::cin >>hex >> input;
        int size = s.size();
        bitset<100000> bitvec(input);
        for (int i = size*4 - 1; i >= 0 ; i -=3)
        {
            
        }
    }
    std::cin >> hex >> input;
    std::cout << oct << input;
    return 0;
}